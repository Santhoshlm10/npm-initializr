import { Label } from "../ui/label";

const PackageInfo = () => {
    return (
        <div>
            <Label className="text-base">Package Details</Label>
        </div>
    )
};

export default PackageInfo;